# API
PORT=4000
NODE_ENV=development
# In production set this to your Render app URL / allowed origins (comma-separated) or * to allow all.
CORS_ORIGIN=*
JWT_SECRET=super_secret_change_me

# MongoDB
# For Render, use a hosted MongoDB (e.g. MongoDB Atlas) connection string.
MONGODB_URI=mongodb://localhost:27017/committee
